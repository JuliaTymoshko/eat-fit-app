import classNames from 'classnames';
import styles from './upline.module.scss';

const Upline = () => {
  return (
    <div className="Upline">
      <div className={classNames(styles.upline)}></div>
    </div>
  );
};

export default Upline;
